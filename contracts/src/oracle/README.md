# Oracle Module — v0.2

Not implemented. Scope per `docs/oracle.md`: node staking, EIP-712 submissions, round lifecycle
(OPEN → QUORUM_MET → SETTLED), and `SLASHER_ROLE`-gated slashing.

Nothing here may be built while v0.1 is in progress.
