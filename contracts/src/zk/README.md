# zk Verifier — v0.4

Not implemented. Scope per `docs/zk.md`: generated verifier contract plus `ZkVaultGate` with a
nullifier map. Verifier contracts are generated by snarkjs/Nargo, never hand-written.
